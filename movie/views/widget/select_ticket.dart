import 'package:flutter/material.dart';
class SelectTickets extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SnackBar(
      content: Text('HI')

    );
  }
}
